# Port the Nest backend listens on
PORT=3001

# CORS allowed origin (use '*' for development, set to your frontend URL in prod)
CORS_ORIGIN=*

# Path Swagger UI is mounted at (http://localhost:PORT/SWAGGER_PATH)
SWAGGER_PATH=docs
